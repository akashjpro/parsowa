import 'package:flutter/material.dart';

class JobRecordWidget extends StatelessWidget {
  const JobRecordWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
