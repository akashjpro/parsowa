import 'package:flutter/material.dart';

class AttendListWidget extends StatelessWidget {
  const AttendListWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
